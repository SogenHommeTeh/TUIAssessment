@using TUI.Data.Flights.Options
@model TUI.Data.Flights.DTOs.FlightDTO.PageDTO
@{
    ViewData["Title"] = "Flights";

    await Html.RenderPartialAsync("Partials/FlightCreate", new FlightPostOptions());

    await Html.RenderPartialAsync("Partials/FlightsList", Model.Data);

    ViewData["Controller"] = "Flights";
    ViewData["Action"] = "GetPage";

    await Html.RenderPartialAsync("~/Views/Common/Partials/Pagination.cshtml", Model, ViewData);
}
